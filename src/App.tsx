import { FC } from "react";
import "./App.css";

const App: FC = () => {
  return (
    <div>
      <h1>This is test of CI-CD</h1>
      <p className="read-the-docs">
        Continuous Delivery (CD) and Continuous Deployment (CD) are both
        practices within the DevOps framework aimed at streamlining the software
        release process, but they differ in their approach to deploying code
        changes.
      </p>
    </div>
  );
};

export default App;
